export enum SampleEnum {
    IsFloorPlan,
    IsNotFloorPlan,
}

export enum SampleEnumWithStrings {
    FloorPlan = "Floor plan",
    Elevation = "Elevation",
    ConstructiveDetail = "Constructive detail",
}

export enum Environments {
    Development = "development",
}
